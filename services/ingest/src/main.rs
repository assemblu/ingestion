#[tokio::main]
async fn main() {
    // Optional direct-insert mode when Kafka disabled.
    // Use idempotency keys and retries with jitter.
    println!("ingest stub (no-Kafka mode docs only)");
}

